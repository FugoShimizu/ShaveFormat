#include "binding.hpp"

/**
 *
 * @param x
 * @return
 */
int External(int x) {
	{
		BIND();
		if(x) if(x) return 1;
	}
	return 0;
}

/**
 *
 * @param x
 * @return
 */
int Included(int x) {
	{
		#include "binding.inc"
		if(x) if(x) return 1;
	}
	return 0;
}
