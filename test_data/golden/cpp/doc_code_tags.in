/**
 * 加算
 * ```text
 * @param fake 甲。乙
 * @return
 * ```
 * @param Second 第二値
 * ```text
 * second-description
 * ```
 * @param First 第一値
 * ```text
 * first-description
 * ```
 * @param obsolete 不要
 * @return 結果
 */
int Example(const int First, const int Second) { return First + Second; }
