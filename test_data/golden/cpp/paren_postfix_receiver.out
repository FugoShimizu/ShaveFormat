struct S {
	int F(int);
	int X;
};

/**
 *
 * @param s
 * @param p
 * @param Pmf
 * @param Pm
 * @return
 */
int f(S s, S *p, int (S::*Pmf)(int), int S::*Pm) {
	int x =
	a.b.c + g().h + a[0].b + p->q->r + (f)(1) + (a.b)(2) + this->x + (new A)->b + a.b.c + (s .* Pmf)(1) + s .* Pm + (p->max)(1, 2);
	return x;
}
