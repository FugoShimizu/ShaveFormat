int x = 1;
decltype((x)) y = x;

/**
 *
 * @param i
 * @param n
 * @return
 */
bool g(int i, int n) {
	return i < (n >> 1);
}

struct S {
	int v = x;
	int a = b;
	int c = d;
};

/**
 *
 * @param c
 * @param b
 * @param d
 */
void h(int c, int b, int d) {
	(c ? b : d) = 5;
}
