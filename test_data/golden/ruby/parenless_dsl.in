class Routes
  def call
    post :play, to: "games#play"
    validates :name, presence: true, length: { maximum: 30 }
    resources :items, param: :public_id, only: %i[index show] do
      member do
        post :like
      end
    end
  end
end
