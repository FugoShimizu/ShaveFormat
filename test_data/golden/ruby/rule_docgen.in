# === rule_docgen_generate ===
def add(a, b)
	return a + b
end

def log_message(message)
	puts message
end

# === rule_docgen_reconcile ===
# 合計を求める
# @param a 第一項
# @param obsolete 余分な引数
# @return 合計
def total(a, b, c)
	return a + b + c
end
