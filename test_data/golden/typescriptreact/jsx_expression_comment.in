const a = (
  <div>
    {x /* c */}
    <span>{y && ( // show
      <B/>
    )}</span>
    {cond ? <A/> : null /* d */}
  </div>
);
const a2 = <div>{x && ( // show
  <B/>
)}</div>;
const b = <div>{x /* c */}</div>;
const c = <div>{cond ? <A/> : null /* c */}</div>;
const d = <>{x /* e */}</>;
const e = <A b={x /* c */} d={y // e
} />;
