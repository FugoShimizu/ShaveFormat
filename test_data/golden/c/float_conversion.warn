8:31: Consider an `F` suffix only if rounding and exceptions are unchanged
18:31: Consider an `F` suffix only if rounding and exceptions are unchanged
28:31: Consider an `F` suffix only if rounding and exceptions are unchanged
38:31: Consider an `F` suffix only if rounding and exceptions are unchanged
48:31: Consider an `F` suffix only if rounding and exceptions are unchanged
58:31: Consider an `F` suffix only if rounding and exceptions are unchanged
68:31: Consider an `F` suffix only if rounding and exceptions are unchanged
78:31: Consider an `F` suffix only if rounding and exceptions are unchanged
98:31: Consider an `F` suffix only if rounding and exceptions are unchanged
108:31: Consider an `F` suffix only if rounding and exceptions are unchanged
