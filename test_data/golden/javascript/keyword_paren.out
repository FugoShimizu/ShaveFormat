/**
 *
 * @param x
 * @returns
 */
async function f(x) {
	await x;
	const y = await g();
	await (a || b);
	return y;
}

function *h() {
	yield 1;
	yield (a, b);
}

/**
 *
 */
function t() {
	throw new Error("e");
}

/**
 *
 * @returns
 */
function u() {
	return a && b;
}

const d = new Date(0);
const e = new a.B;
const g = new (a.b());
const k = new (a?.b);
const m = new a["B"];
const n = new a.arr[0];

for(k in a = o) {}
