/**
 *
 * @param e
 * @param r
 * @returns
 */
function f(e, r) {
	return e[0] + "e" + (+e[1] + r);
}

/**
 *
 * @param value
 * @returns
 */
function g(value) {
	return "new/" + value + "/" + ++X.uniquifier;
}
